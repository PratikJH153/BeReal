import 'package:flutter/material.dart';

class NamePage extends StatelessWidget {
  static const routeID = "/namepage";
  const NamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
